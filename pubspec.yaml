name: billtrack
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  image_picker: ^1.1.2
  http: ^1.3.0
  provider: ^6.1.2
  get_it: ^8.0.2
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  shimmer: ^3.0.0
  path_provider: ^2.1.4
  permission_handler: ^11.3.1
  image_cropper: ^8.0.2
  flutter_form_builder: ^10.3.0
  form_builder_validators: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
